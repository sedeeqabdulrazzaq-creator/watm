# إصلاحات WATM الاحترافية — المرحلة الأولى

هذه النسخة ناتجة عن مراجعة `WATM.zip`، وتركّز على أخطاء P0 التي تؤثر مباشرة
في صدق التجربة واستقرارها.

## ما تم إصلاحه

- أصبح تسجيل اليوم ينتهي بشاشة نجاح ثم يعود إلى لوحة العضو الحقيقية.
- أزيلت شاشات الأشخاص والنسب التجريبية من مسار الاستخدام.
- أصبح شريط التنقل ثابتاً أسفل لوحة العضو ومتجاوباً مع عرض الهاتف.
- أزيل مسار الهاتف الذي كان يقبل أي رمز من ستة أرقام.
- بقي التسجيل الحقيقي بالبريد الإلكتروني، وأضيفت استعادة كلمة المرور.
- أضيفت مستقبلات تنبيهات Android وإذن استعادة التنبيه بعد إعادة التشغيل.
- لم تعد التنبيهات تُلغى عند التنقل بعيداً عن لوحة التحكم.
- أضيف إعداد إشعارات iOS المطلوب في `AppDelegate.swift`.
- استبدل اختبار العداد الافتراضي باختبار WATM حقيقي، وصُحح نص اختبار البداية.
- لم تعد سكربتات التشغيل تحذف الاختبارات أو تتجاهل فشلها.
- شُددت قواعد المطابقة لمنع الانضمام إلى دائرة لا تطابق المفتاح المؤقت للحساب.
- أصبح اسم مرسل التشجيع مطابقاً لسجل العضو داخل الدائرة.
- أضيفت معالجة خطأ واضحة عند تعذر تحميل أعضاء الدائرة.
- استبدلت أسماء ووصف Flutter الافتراضي في الويب وiOS بهوية WATM.

## التحقق على جهاز التطوير

نفّذ من مجلد المشروع:

```powershell
flutter pub get
flutter analyze
flutter test --concurrency=1
flutter run -d chrome
```

ولإنشاء نسخة Android تجريبية:

```powershell
flutter build apk --release
```

## ما لم يُقدَّم كميزة مكتملة بعد

- الدفع الحقيقي وبوابة الدفع العراقية.
- حذف الحساب والبيانات السحابية؛ يحتاجان عملية خلفية آمنة.
- نقل المطابقة والاشتراك واحتساب الاستمرار بالكامل إلى Cloud Functions.
- توقيع Google Play الإنتاجي والمعرّف النهائي وأيقونة WATM.
- إشعارات دفع FCM عند وصول تشجيع والتطبيق مغلق.

لا ينبغي الإعلان عن هذه العناصر للمستخدم على أنها مكتملة قبل تنفيذها واختبارها.
