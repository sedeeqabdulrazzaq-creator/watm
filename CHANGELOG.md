# سجل التغييرات — WATM

## أسئلة انضمام أعمق وأكثر احترافية

- إعادة صياغة أسئلة طلب الانضمام الثمانية لتكشف الدافع الحقيقي، نمط
  التعثر، الحد الأدنى للالتزام، أسلوب الدعم، وحدود الخصوصية.
- تحويل الأسئلة من جمع معلومات عامة إلى تجربة تأملية تحترم المشترك وتوضح
  قيمة المطابقة.
- تطوير أسئلة ملف العضوية لإبراز قيمة العضو داخل الدائرة، ووضع طريق واضح
  للعودة بعد التعثر، وتحديد حدود المتابعة باحترام.
- تحسين ميثاق الدائرة والالتزامات الشخصية لتصبح وعوداً عملية قابلة
  للاستمرار بدلاً من أهداف مثالية صعبة.

## رحلة خسارة الوزن — الإصدار الأول

- تخصيص رسالة WATM ورحلة التسجيل لخسارة الوزن فقط.
- إضافة الوزن الحالي والوزن المستهدف مع إبقاء الرقمين خاصين بالمستخدم.
- حساب مقدار الخسارة تلقائياً وتصنيف المستخدم ضمن نطاق مناسب للمطابقة.
- مطابقة الدوائر بحسب نطاق خسارة الوزن والمدة ووقت التفاعل.
- تحويل تسجيل اليوم إلى ثلاثة التزامات واضحة: الحركة وخطة الطعام والماء.
- عرض نتيجة الالتزامات اليومية في الرئيسية والتقدم الأسبوعي.
- عزل التقدم المحلي بين الحسابات حتى يبدأ الحساب الجديد رحلته من البداية.
- تشديد قواعد Firestore للتحقق من تفاصيل تسجيل الالتزام الجديدة.

## الجدول اليومي والأسبوعي

- إضافة تبويب «جدولي» في منتصف شريط التنقل.
- إنشاء مواعيد يومية أو أسبوعية مع اختيار اليوم والوقت.
- تعديل الموعد أو حذفه وتشغيل المنبّه أو إيقافه.
- حفظ المواعيد لكل مستخدم داخل Firestore.
- إشعارات محلية متكررة على Android وiOS، وتنبيهات المتصفح أثناء بقاء
  التطبيق مفتوحاً على Chrome.

## تسجيل الخروج

- إضافة زر «تسجيل الخروج من التطبيق» داخل تبويب «حسابي».
- عرض نافذة تأكيد لمنع الخروج بالخطأ.
- إنهاء جلسة Firebase والعودة تلقائياً إلى شاشة تسجيل الدخول.
- الاحتفاظ بالدائرة والتقدم وبيانات العضوية في Firestore بعد الخروج.

## المطابقة التلقائية وتكوين الدوائر

- إنشاء دائرة تلقائياً بحسب الهدف والمدة ووقت التفاعل.
- ضم المستخدم إلى دائرة مفتوحة متوافقة من دون اختيار يدوي.
- حد أقصى 7 أعضاء، وتتحول الدائرة إلى نشطة عند 5 أعضاء ومكتملة عند 7.
- استخدام معاملة Firestore لمنع تجاوز الحد عند الانضمام المتزامن.
- عرض أعضاء Firestore الحقيقيين بدلاً من بيانات العرض الثابتة.
- تحديث نقاط الاستمرارية في مستند عضو الدائرة بعد كل تسجيل يومي.

توثيق تاريخي لمراحل بناء التطبيق، مُجمَّع من ملفات `README-V3` … `README-V10`
و`MANUAL-FIX-AR.md` السابقة (تم دمجها هنا والإبقاء على ملف واحد لتفادي
التشتت). للتشغيل والإعداد الحالي راجع `README.md`.

## V3 — Firebase Circle

- تسجيل وإنشاء الحساب بالبريد الإلكتروني وكلمة المرور عبر Firebase الحقيقي.
- تجربة مجانية لمدة 7 أيام بلا بطاقة أو دفع تلقائي.
- الصفحة الرئيسية تقرأ الاسم وحالة التجربة من `users/{uid}`.
- تسجيل اليوم يقرأ من `users/{uid}/checkIns/{yyyy-MM-dd}`.
- الدائرة تقرأ من `groups/{groupId}` وأعضاؤها من `groups/{groupId}/members`.
- عند عدم تعيين دائرة تظهر حالة «مطابقة دائرتك قيد الإعداد» بدلاً من أعضاء
  وهميين.
- تعيين دائرة لمستخدم: أضف حقل `groupId` (نصي) داخل مستند المستخدم في
  مجموعة `users`، بقيمة تساوي معرّف مستند الدائرة في `groups`.

## V5 — Firebase Navigation

الصفحات المرتبطة بـ Firebase:

- الرئيسية: حالة تسجيل اليوم وملخص الدائرة.
- دائرتي: بيانات الدائرة وأعضاؤها الحقيقيون.
- التقدم: عدد تسجيلات الأسبوع وسجل الحضور.
- حسابي: البريد وحالة التجربة والمدينة والخصوصية.
- تسجيل اليوم: يفتح رحلة التسجيل الحالية ويحفظها في Firestore.

## V6 — Firebase Encouragement

- اختيار عضو حقيقي من أعضاء الدائرة.
- أربع رسائل تشجيع قصيرة ومحترمة.
- حفظ التشجيع داخل `groups/{groupId}/encouragements`.
- عرض آخر خمسة تشجيعات داخل صفحة الدائرة.
- منع المستخدم من إرسال التشجيع لنفسه.
- صلاحيات التشجيع مضمّنة في ملف `firestore.rules` الموحد.

## V7 — إرسال التشجيع الآمن

يعالج توقف شاشة التشجيع عند رفض Firestore للكتابة داخل الدائرة:

- تُحاوَل أولاً كتابة التشجيع داخل `groups/{groupId}/encouragements`.
- إذا رفضت القواعد العملية، تُحفظ الرسالة في قائمة آمنة داخل حساب المرسل:
  `users/{uid}/encouragementOutbox`.
- يُعرض رمز خطأ Firebase الحقيقي عند فشل المسارين، لتسهيل التشخيص.

## V8 — مركز التشجيعات

- بطاقة على الصفحة الرئيسية بعدّاد التشجيعات الجديدة.
- صفحة تعرض التشجيعات المستلمة فقط.
- نقطة واضحة للرسالة الجديدة.
- الضغط على الرسالة يحددها كمقروءة، وزر لتحديد الجميع كمقروء.
- إيصالات القراءة تُحفظ داخل `users/{uid}/notificationReads` دون فهرس إضافي.

## V9 — المراجعة الأسبوعية

رحلة مراجعة أسبوعية قصيرة داخل تبويب «التقدم»:

- ملخص حضور آخر أسبوع.
- تقييم الالتزام من 1 إلى 5.
- اختيار أهم انتصار صغير.
- تحديد تركيز الأسبوع القادم.
- تُحفظ وتُحدَّث المراجعة في `users/{uid}/weeklyReviews/{weekId}`.
- لا تحتاج هذه الميزة إلى فهرس Firestore أو تعديل إضافي للقواعد الحالية.

## V10 — العودة بعد الغياب

رحلة عودة داعمة بلا لوم:

- يكتشف التطبيق الغياب تلقائياً من سجل الحضور.
- تظهر بطاقة «مكانك محفوظ» بعد فوات يوم كامل.
- يختار العضو خطوة رجوع بسيطة.
- تُحفظ العودة في `users/{uid}/returns/{date}`.
- زر «معاينة العودة بعد الغياب» في تبويب التقدم لاختبار الرحلة فوراً.
- لا تحتاج الميزة إلى تعديل جديد في قواعد Firestore الحالية.

## إصلاح يدوي — الاستئناف الصحيح عند فتح التطبيق

إصلاح لمشكلة فتح التطبيق مباشرة على شاشة «من يحتاج دفعة اليوم؟» بدل الشاشة
الرئيسية:

- العضو في التجربة المجانية أو العضوية الفعالة يعود إلى الشاشة الرئيسية.
- شاشات تسجيل اليوم والتشجيع والمراجعة الأسبوعية لا تُحفظ كشاشة بدء.
- إذا انتهت التجربة المجانية، تظهر شاشة انتهاء التجربة.
- يُصحَّح رقم الخطوة القديم المحفوظ محلياً وفي Firestore تلقائياً عند
  التشغيل.
- بوابة البريد تراقب حالة Firebase مباشرة؛ عند تسجيل الخروج تعود شاشة
  الدخول، وعند الدخول بحساب مختلف تبدأ نسخة مستقلة من رحلة ذلك الحساب.
- زر «لدي حساب» في شاشة الترحيب يعمل الآن وينقل إلى تسجيل الدخول.

هذا المنطق موجود اليوم في الدالة `normalizeMvpRestoredStep` داخل
`lib/features/mvp/presentation/mvp_flow_helpers.dart`، وله اختبار مخصص في
`test/mvp_restore_step_test.dart`.

## إصلاح بدء التجربة والاختبارات

- تصحيح مسار استيراد اختبار استعادة الشاشة إلى اسم الحزمة `watm_app`.
- بدء التجربة المجانية محلياً فوراً ثم مزامنتها مع Firestore في الخلفية، حتى
  لا يتوقف زر «ابدأ تجربتي المجانية» عند بطء الاتصال أو تعذر الكتابة مؤقتاً.
- إعادة محاولة المزامنة تلقائياً عند فتح التطبيق إذا لم تصل بداية التجربة
  إلى Firestore في المحاولة السابقة.
- منع قيمة `applicant` القديمة في Firestore من إلغاء تجربة محلية بدأت بنجاح.
- إضافة `key` إلى جميع Widgets العامة في رحلة MVP لإزالة ملاحظات
  `use_key_in_widget_constructors` من فحص Flutter.

## إصلاح فقدان رقم الهاتف عند إعادة تسجيل الدخول

- `ensureUserDocument()` كانت تكتب `'phone': null` في كل مرة (لأن التطبيق
  يعتمد على البريد وكلمة المرور فقط ولا يستخدم مصادقة الهاتف الحقيقية أبداً)،
  وبما أن الكتابة تستخدم `SetOptions(merge: true)`، فإن وجود المفتاح بقيمة
  `null` كان يمحو رقم الهاتف المحفوظ سابقاً بدل تجاهله.
- الآن لا يُكتب مفتاح `phone` إلا عند وجود رقم فعلي من Firebase Auth، أو عند
  تنظيف القيمة الوهمية القديمة `'+'`، فيبقى الرقم الصحيح محفوظاً عبر جلسات
  الدخول المتكررة.
- أُضيف تحقق لحقل «العمر» في شاشة المعلومات الأساسية (13–100 سنة) بنفس
  أسلوب التحقق من الاسم والمدينة، بدل قبول قيمة فارغة أو غير رقمية بصمت.

## إصلاح شاشة «كيف تريد المتابعة؟» المعطّلة

- كان خياري «المتابعة برقم الهاتف» و«المتابعة بالبريد الإلكتروني» ثابتَين
  في الشيفرة (`selected` و`onTap: () {}` بلا أي حالة فعلية)، فلا يتغيّر شيء
  عند الضغط على أي منهما، وزر «متابعة» يكمل دائماً لنفس المسار.
- أصبح الاختيار مرتبطاً بحالة حقيقية (`_joinByPhone`)، واختيار «البريد
  الإلكتروني» يتخطّى الآن خطوتَي إدخال الهاتف والتحقق بالكامل وينتقل مباشرة
  لشاشة المعلومات الأساسية، لأن المصادقة الفعلية أصلاً تتم بالبريد عبر
  `FirebaseEmailGate` قبل الوصول لهذه الشاشة.
- صُحِّح زر الرجوع في شاشة المعلومات الأساسية ليعود للشاشة الصحيحة حسب
  المسار الذي اختاره العضو (الهاتف أو البريد)، بدل الرجوع دائماً خطوة واحدة
  فقط.

## دائرة بحد أقصى 7 أعضاء + كابتن الدائرة حسب النقاط

- أُضيف ثابت مشترك `kCircleMaxMembers = 7` في
  `lib/core/utils/circle_settings.dart`، يُستخدم الآن في استعلامات أعضاء
  الدائرة (تبويب «دائرتي» وشاشة اختيار عضو للتشجيع) وفي عرض الحد الأقصى داخل
  بطاقة الدائرة، بدل الاعتماد فقط على حقل `maxMembers` في Firestore أو رقم
  9 ثابت سابقاً في الاستعلامات.
- إن كان مستند الدائرة يحدد `maxMembers` صراحةً فتُستخدم قيمته، وإلا يُستخدم
  الحد الافتراضي 7.
- أُضيف شارة «كابتن الدائرة» تظهر تلقائياً بجانب اسم العضو الأعلى نقاطاً
  داخل تبويب «دائرتي». بما أنه لا يوجد حقل نقاط مخصص بعد في بيانات
  Firestore، اعتُمد `personalStreak` (أيام الالتزام المتتالية) كمقياس
  النقاط الحالي — وهو محسوب ديناميكياً في كل مرة تتغيّر فيها البيانات، وليس
  عضواً ثابتاً. لا تظهر الشارة إطلاقاً إن لم يحقق أي عضو نقاطاً بعد.
