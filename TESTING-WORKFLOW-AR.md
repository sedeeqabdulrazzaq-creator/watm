# WATM — بيئة التطوير والاختبار مقابل الإنتاج

هذا الملف يوثّق التنظيم الجديد بعد مشكلة "الأعضاء الأشباح" (أعضاء اختبار حُذفوا من
Authentication لكن بقيت بياناتهم بـ Firestore). الهدف: لا يعود التطوير أو الاختبار
يلمس بيانات الإنتاج الحقيقية إطلاقاً.

## القاعدة

- **أي تطوير أو اختبار محلي → Firebase Emulators.** بيانات مؤقتة، بجهازك فقط،
  تُمسح بضغطة زر بدون أي أثر على الإنتاج.
- **الإنتاج (`watm-429c3`) → للمستخدمين الحقيقيين فقط.** لا تسوي حسابات اختبار
  عليه بعد اليوم، ولا تحذف حسابات من كونسول Authentication الحقيقي إلا إذا كان
  طلب حذف حساب حقيقي فعلي.

## التشغيل اليومي

افتح ترمينالين داخل مجلد `watm`:

**ترمينال 1 — شغّل الـ emulators:**

```powershell
.\start-emulators.ps1
```

يفتح واجهة مراقبة على `http://localhost:4000` تقدر تشوف منها كل حسابات
Authentication ومستندات Firestore المحلية، وتحذفها مباشرة من هناك بدون قلق —
كلها بيانات مؤقتة بجهازك.

**ترمينال 2 — شغّل التطبيق مربوط بالـ emulators:**

```powershell
.\start-dev.ps1 -Emulator
```

بدون `-Emulator` (`.\start-dev.ps1` لحاله) يرجع يشتغل على الإنتاج الحقيقي —
استخدمها بس لما فعلاً تحتاج تتحقق من شي على البيانات الحقيقية، ليس للتجربة
العشوائية.

## تصفير بيانات الاختبار

فقط احذف مجلد `.emulator-data` (أو لا تصدّره) وأعد تشغيل `start-emulators.ps1` —
تبدأ من صفر بدون فتح Firebase Console إطلاقاً.

## شبكة الأمان على الإنتاج (بالإضافة للـ emulators، مو بديل عنها)

حتى لو صار حذف حساب حقيقي من الإنتاج مستقبلاً، دالة `functions/index.js`
(`cleanupUserOnDelete`) تنظّف تلقائياً بيانات ذلك الحساب من Firestore وعضويته
من دائرته. لازم تكون منشورة:

```powershell
firebase deploy --only functions,firestore:rules
```

إذا صار عندك يوماً بيانات يتيمة على الإنتاج قبل نشر هذه الدالة (نفس مشكلتنا)،
سكربت `scripts/cleanup-orphaned-members.js` يسوي تنظيف لمرة وحدة — راجع
التعليقات بأول الملف لطريقة التشغيل، وشغّله دائماً بدون `--apply` أولاً
للمراجعة قبل التنفيذ الفعلي.

## قائمة تحقق قبل أي نشر للإنتاج

```powershell
flutter analyze
flutter test --concurrency=1
firebase deploy --only firestore:rules,functions
flutter build apk --release
```
