# WATM — إصلاحات المرحلة الثانية

هذه المرحلة تركّز على صدق تدفق المستخدم، سلامة البيانات، وتجربة تشغيل يمكن الاعتماد عليها على الويب وأندرويد.

> تحديث 2.0.3: لن تبقى شاشة المطابقة في التحميل إلى ما لا نهاية. تنتهي محاولة Firestore خلال مهلة محددة، ثم تظهر رسالة واضحة مع إعادة المحاولة أو الرجوع، من دون إنشاء عضوية مكررة.

> تحديث 2.0.2: أصبحت هوية الدائرة واضحة من الهدف والمدة ووقت التفاعل، مع رمز مرجعي ثابت يميّز أي دائرتين متشابهتين بالاسم. لم تُحذف أو تُدمج بيانات حالية.

> تحديث 2.0.1: تصحيح null-safety عند استعادة إجابات المراجعة الأسبوعية حتى تُترجم النسخة على Flutter من دون خطأ `String?`.

## ما تم إصلاحه

### 1. الدخول وإنشاء الحساب

- يبدأ التطبيق بشاشة WATM الترحيبية بدلاً من إجبار الزائر على التسجيل فوراً.
- زر **لدي حساب** يفتح وضع تسجيل الدخول، بينما **ابدأ الطلب** يفتح وضع إنشاء الحساب.
- يمكن الرجوع من شاشة الدخول أو إنشاء الحساب إلى تعريف الرحلة من دون احتجاز المستخدم داخل النموذج.
- بعد نجاح المصادقة يستعيد التطبيق تقدم الحساب الصحيح من Firestore.
- تُمسح الإجابات الموجودة في الذاكرة قبل استعادة حساب آخر لمنع انتقال بيانات مستخدم إلى مستخدم آخر على الجهاز نفسه.
- يبقى استرجاع كلمة المرور متاحاً من شاشة الدخول.

### 2. تسجيل الحضور اليومي

- لا يعرض التطبيق رسالة نجاح قبل أن يحفظ Firestore التسجيل فعلياً.
- وُصفت ملاحظة تسجيل اليوم بوضوح على أنها خاصة بالمستخدم وليست منشوراً للدائرة.
- يُحسب التسلسل اليومي من بيانات اليوم السابق في Firestore، وليس من رقم محلي قابل للخطأ.
- إعادة تعديل تسجيل اليوم لا تزيد التسلسل مرة ثانية.
- تظهر رسالة واضحة عند انقطاع الإنترنت أو رفض الصلاحيات مع إمكانية إعادة المحاولة.
- تُحدَّث بيانات عضو الدائرة فقط إذا كان سجل العضو موجوداً، حتى لا تمنع بيانات قديمة ناقصة المستخدم من تسجيل يومه الخاص.

### 3. التقدم والمراجعة الأسبوعية

- أصبح الأسبوع موحداً من الاثنين إلى الأحد في شاشة التقدم والمراجعة الأسبوعية.
- تستخدم المراجعة معرفاً ثابتاً للأسبوع، وتستعيد الإجابات المحفوظة عند فتحها مرة أخرى.
- أضيفت حالات تحميل وفشل واضحة بدلاً من عرض صفر أو شاشة مضللة أثناء انتظار البيانات.
- أضيفت اختبارات لحدود الأسبوع ومعرّفات الأيام وحساب التسلسل.

### 4. الدائرة والتشجيعات

- أضيفت مهلة 25 ثانية لعمليات المطابقة ومعاملة Firestore الذرية، لمنع شاشة تحميل معلقة عند ضعف الاتصال.
- بعد انتهاء المهلة يستطيع المستخدم إعادة المحاولة أو العودة إلى السؤال السابق، وتبقى العملية آمنة من تكرار العضوية.
- أصبح اسم الدائرة المعروض مبنياً على معايير المطابقة الثلاثة نفسها: هدف النزول، المدة، ووقت التفاعل.
- أضيف رمز دائرة ثابت في شاشة الدائرة والملخص وشاشة نجاح المطابقة، لتأكيد أن الحسابات تشاهد الدائرة نفسها فعلاً.
- الحسابات ذات المعايير الثلاثة المتطابقة تستعمل مفتاح مطابقة واحداً؛ اختلاف المدة أو وقت التفاعل ينشئ دائرة مختلفة بوضوح.
- يتحقق التطبيق من اسم المرسل الحقيقي داخل سجل أعضاء الدائرة قبل إرسال التشجيع.
- تمنع القواعد انتحال اسم المرسل أو اسم المستلم.
- أزيل الحفظ الوهمي في صندوق إرسال لا توجد له خدمة خلفية؛ النجاح يظهر فقط بعد وصول الكتابة إلى Firestore.
- تظهر حالات تحميل وفشل مفهومة عند قراءة الأعضاء والتشجيعات.

### 5. الإشعارات

- يجلب التطبيق التشجيعات المرسلة إلى المستخدم الحالي فقط.
- تُحدَّث سجلات القراءة للإشعارات غير المقروءة فقط.
- جرى تقسيم «تحديد الكل كمقروء» إلى دفعات صغيرة متوافقة مع حدود فحص قواعد Firestore.
- تتحقق القواعد من أن الإشعار المشار إليه موجود وأن المستخدم الحالي هو المستلم الحقيقي.

### 6. العضوية والتسعير

- 21 يوماً: **9,999 د.ع**
- 3 أشهر: **24,999 د.ع**
- سنة: **79,999 د.ع**
- لا يدّعي التطبيق تنفيذ دفع حقيقي في النسخة التجريبية، ولا يُظهر رسالة خصم وهمية.

### 7. إعداد Firebase

- أضيف ملف `.firebaserc` وربط المشروع الافتراضي بـ `watm-429c3`.
- يمكن الآن نشر القواعد من مجلد المشروع من دون كتابة رقم المشروع كل مرة.

## الفحص على Windows

افتح PowerShell داخل مجلد `watm` ثم نفّذ بالترتيب:

```powershell
flutter pub get
flutter analyze
flutter test --concurrency=1
firebase deploy --only firestore:rules
flutter run -d chrome
```

## إنشاء نسخة أندرويد

```powershell
flutter build apk --release
```

ستجد ملف APK في:

```text
build\app\outputs\flutter-apk\app-release.apk
```

> يجب نشر ملف القواعد الموجود في هذه النسخة؛ فهو يتضمن تحقق الحضور اليومي والمراجعة الأسبوعية والتشجيعات والإشعارات.
