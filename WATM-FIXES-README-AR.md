# WATM — النسخة المُصلَحة الكاملة

## ⚠️ اقرأ هذا أولاً: ملفان ناقصان عمداً

هذه الحزمة **لا تحتوي**:

| الملف | لماذا | ماذا تفعل |
|---|---|---|
| `lib/firebase_options.dart` | يحوي مفاتيح مشروعك التي وُلّدت عندك بـ`flutterfire configure`. أي نسخة عندي ستكون قديمة وتكسر التطبيق. | احتفظ بنسختك |
| `android/key.properties` | يحوي كلمتَي مرور مفتاح التوقيع | احتفظ بنسختك |

### الطريقة الصحيحة للتثبيت

فُكّ الحزمة **فوق** مجلد مشروعك الحالي:

```powershell
Expand-Archive -Path "$HOME\Downloads\watm-complete.zip" -DestinationPath C:\watm -Force
```

الملفان أعلاه غير موجودين في الحزمة، فلن يُمسّا. تحقّق بعدها:

```powershell
cd C:\watm
Test-Path lib\firebase_options.dart, android\key.properties
flutter clean; flutter pub get; flutter analyze
```

المتوقّع: `True` و`True` ثم `No issues found!`

### إن بدأت من مجلد فارغ

```powershell
flutterfire configure --project=watm-429c3
Copy-Item android\key.properties.example android\key.properties
notepad android\key.properties
```

---

## ما تغيّر

### ① مشاكل تمنع النشر

| قبل | بعد |
|---|---|
| `applicationId = "com.example.watm_app"` — يرفضه Google Play | `com.watm.watm` |
| نسخة الإصدار موقّعة بمفتاح التصحيح | توقيع حقيقي من `android/key.properties`، مع تحذير مطبوع إن غاب الملف |
| لا `POST_NOTIFICATIONS` — التذكيرات لا تعمل على Android 13+ | أُضيفت |
| حزمة Kotlin `com/example/watm_app` | `com/watm/watm` |
| `PRODUCT_BUNDLE_IDENTIFIER = com.example.watmApp` | `com.watm.watm` في المواضع الستة |

### ② التأخير والأداء

**`lib/core/utils/live_query.dart` — ملف جديد.** كل `.snapshots()` كان يُستدعى داخل `build()`، و`snapshots()` يُرجع كائن تدفق جديد في كل مرة، فيلغي `StreamBuilder` اشتراكه ويعيده عند كل إعادة بناء: قراءات مفوترة مكررة، ووميض مؤشرات تحميل فوق بيانات موجودة أصلاً. `LiveQuery` يحتفظ بمستمع واحد لكل مسار ويعيد نفس الكائن دائماً.

استُبدل بها **٢٢ نداءً** في تسعة ملفات. مكسب جانبي: المواضع التي تقرأ نفس البيانات صارت تتشارك مستمعاً واحداً — `members` كان له أربعة، صار واحداً.

**شاشة البداية.** كان مؤقّت الـ١٥٠٠ مللي يبدأ **بعد** انتهاء استعادة الملف الشخصي، فمدة الشاشة = زمن الشبكة + ١.٥ ثانية. الآن يعملان بالتوازي، و`loadProfile()` عليها مهلة ٦ ثوانٍ (كانت بلا مهلة إطلاقاً، فكان الإقلاع يعلق كلياً على اتصال متقطّع).

**حدود الاستعلامات:**

| أين | قبل | بعد |
|---|---|---|
| لافتة الغياب | كل السجل | `limit(1)` |
| المراجعة الأسبوعية | كل السجل | `limit(31)` |
| `scheduleItems` (٣ مواضع) | بلا حد | `limit(50)` |

استُخدم `orderBy(FieldPath.documentId)` لأن معرّفات التسجيلات بصيغة `YYYY-MM-DD` فترتيبها الأبجدي هو الزمني — لا يحتاج أي فهرس جديد.

**تفريغ عند تبديل الحساب** في `main.dart`، وإلا بقي مستمعو الحساب السابق يعملون ضد قواعد ترفضهم.

### ③ قواعد Firestore

معاملة المطابقة كانت تستدعي `get`/`getAfter`/`exists` نحو **٢٢ مرة**، وسقف Firestore **٢٠** للمعاملة و**١٠** للعملية الواحدة. أي تجاوز يعطي `permission-denied` بلا تفسير مفيد.

| التغيير | قبل | بعد |
|---|---|---|
| `userWillPointToGroup` | ٢ | ١ |
| `groupCountWillIncludeNewMember` | ٥ | ٢ |
| `validOpenGroup` + `bucketMatchesCurrentUser` → `validBucketTarget` | ٥ | ٣ |
| `matchingQueue` | ٣ | ٠ |

**المجموع: ~٢٢ ← ~١٢**، وأقصى عملية ٤ بدل ٧.

كيف بلا خسارة أمنية:

- `userWillPointToGroup` كان يقرأ وثيقة المستخدم مرتين للتحقق من `groupId` و`circleId`. قاعدة `users` تفرض أصلاً تساويهما، فواحد يكفي.
- الدائرة الجديدة عدّادها `1` دائماً بحكم قاعدة `create`، فالمسار الأول يقصر دائرياً بلا قراءة الحالة السابقة.
- دالتا الحاوية كانتا تقرآن **نفس وثيقة المجموعة** ثلاث مرات لأن `openGroupId` مُلزَم بمساواة `groupId`. الدمج مع `let` — وهو يُقيَّم مرة واحدة — يجعلها استدعاءً واحداً.
- `matchingQueue` وثيقة خاصة بالمستخدم لا يقرأها أحد. تحقّقها المتقاطع كان كلفة صافية.

**`functions/index.js`:** كان يترك دائرة بعدّاد صفر عند خروج آخر عضو — حالة تخالف القواعد وتوجّه القادمين إلى دائرة شبح. الآن يحذفها ويفرّغ إشارة الحاوية، لكن **فقط إن كانت تشير إليها هي**.

**`firestore.indexes.json`** أُنشئ ورُبط في `firebase.json`.

### ④ تعليق شاشة المطابقة ضد المحاكي

`main.dart` كان يتصل بـ`localhost`، ومحاكيات Firebase تستمع على **IPv4 فقط**، بينما يحلّ المتصفح على ويندوز `localhost` إلى `::1` (IPv6). النتيجة اتصال لا يصل: تدخل Firestore وضع «غير متصل» فتصطف الكتابات محلياً، ولا تُرجع `set()` ولا `runTransaction` أي نتيجة ولا خطأ — تعليق صامت بلا أي سطر رفض في سجل المحاكي.

صار `127.0.0.1` صراحةً.

**لوحة تتبّع على شاشة المطابقة** تعرض آخر خطوة اكتملت داخل المعاملة (في وضع المحاكي فقط)، فلقطة واحدة تكفي لتحديد موضع أي توقّف مستقبلي.

**مهلة الأمان ١٥٠ ← ٤٠ ثانية.** المدة الطويلة كانت تجعل أي فشل يبدو تعليقاً أبدياً.

**رسالة الخطأ الأصلية** صارت تُعرض في وضع المحاكي بدل إخفائها خلف نص عام.

---

## الخطوة المعلّقة الوحيدة

**قواعد Firestore لم تُنشر بعد على المشروع.** هذا ما يمنع المطابقة فعلياً.

```powershell
cd C:\watm
firebase deploy --only firestore:rules --project watm-429c3
```

`--only firestore:rules` مقصود: يتجاوز `functions` التي لا تُنشر على خطة Spark المجانية.

المتوقّع:
```
✔  cloud.firestore: rules file firestore.rules compiled successfully
✔  Deploy complete!
```

---

## مسائل معروفة لم تُعالَج

**تزاحم `matchingBuckets/{key}`.** كل من يحمل نفس مفتاح المطابقة يكتب على وثيقة واحدة، وحد Firestore ~كتابة واحدة/ثانية للوثيقة. الحل الصحيح نقل المطابقة إلى Cloud Function، وهو يتطلب خطة Blaze. عملياً لن تلاحظ شيئاً حتى يسجّل عدة أشخاص بنفس المفتاح في نفس الثانية.

**استعلام `checkIns` في تبويب التقدم بلا حد** — يعرض «إجمالي تسجيلاتك» فتحديده يكسر الرقم. الحل الصحيح عدّاد في وثيقة المستخدم يُزاد في نفس معاملة `saveCheckIn`، ويستلزم تعديل القواعد.

**اسم الدائرة يختلف بين الويب والهاتف.** `circle_settings.dart` يستخدم `seed.hashCode`، ودالة `String.hashCode` في Dart ليست موحّدة بين dart2js والـVM. عضوان في نفس الدائرة، أحدهما على المتصفح والآخر على أندرويد، يريان اسمين مختلفين.

**المنطقة الزمنية مثبّتة على بغداد** في `reminder_service_native.dart`. أي مستخدم خارج العراق يستلم تذكيراته في الوقت الخطأ.

**تذكيرات الويب لا تعمل فعلياً** — مجرد `Timer` داخل الصفحة، يتوقف عند إغلاق التبويب. و`dart:html` مهجور ولن يعمل مع `flutter build web --wasm`.

**لا تحقق من البريد الإلكتروني** بعد إنشاء الحساب — أي بريد وهمي يُنشئ حساباً كاملاً.

**الخطوط تُنزَّل وقت التشغيل.** مجلد `assets/google_fonts/` جاهز — ضع فيه ملفات Alexandria بصيغة TTF (راجع الـREADME بداخله) وستتوقف حزمة `google_fonts` عن الاتصال بالشبكة تلقائياً. اختياري تماماً.
