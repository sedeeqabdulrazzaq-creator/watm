# مشروع WATM الرئيسي داخل Antigravity

هذا هو المجلد الوحيد المعتمد للبرمجة من الآن:

`watm_app_antigravity`

## التشغيل لأول مرة

افتح المجلد نفسه داخل Antigravity، ثم افتح Terminal داخله وشغّل:

```powershell
powershell -ExecutionPolicy Bypass -File .\start-dev.ps1
```

بعد فتح Chrome:

- اضغط `r` داخل Terminal لتنفيذ Hot Reload.
- اضغط `R` لتنفيذ Hot Restart.
- اضغط `q` لإيقاف التطبيق.

## قواعد العمل

1. لا تنشئ مشروع Flutter جديداً.
2. لا تعمل داخل Downloads أو نسخة ZIP أخرى.
3. عدّل الملفات داخل هذا المجلد فقط.
4. شغّل `flutter analyze` و`flutter test` قبل اعتماد أي مرحلة.
5. حافظ على اتجاه RTL والنصوص العربية.
6. حافظ على مشروع Firebase: `watm-429c3`.
7. التجربة المجانية 7 أيام بلا بطاقة أو دفع تلقائي.
8. لا تغيّر ألوان الهوية:
   - `#5FA9D3` الإجراءات الأساسية.
   - `#D7DB62` الإنجازات والتحفيز.
   - `#A8AD32` التقدم والحالات المكتملة.
   - `#75644A` النص الثانوي.
   - `#101820` النص الأساسي والعناوين.
   - `#FBFAF6` الخلفية.

## رسالة جاهزة لوكيل Antigravity

انسخ هذه الرسالة في محادثة Antigravity عند بدء العمل:

> هذا هو مشروع WATM الرئيسي. لا تنشئ مشروعاً جديداً ولا تستبدل بنية المشروع. افحص الكود الحالي أولاً، وحافظ على Firebase project watm-429c3، واتجاه RTL، والهوية اللونية الموجودة. نفّذ التعديلات داخل هذا المجلد فقط. بعد كل تعديل شغّل flutter analyze وflutter test، ثم استخدم Hot Reload لمعاينة النتيجة. لا تضف بوابة دفع؛ التجربة المجانية 7 أيام بلا بطاقة.

## الميزات الموجودة حالياً

- تسجيل ودخول بالبريد الإلكتروني عبر Firebase.
- تجربة مجانية 7 أيام.
- الملف الشخصي والدائرة والأعضاء.
- تسجيل الحضور اليومي والتقدم.
- إرسال التشجيعات واستقبالها.
- مركز إشعارات مع حالة مقروء/جديد.
- المراجعة الأسبوعية.
- رحلة العودة بعد الغياب.
