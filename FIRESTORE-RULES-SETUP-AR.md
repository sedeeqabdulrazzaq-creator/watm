# قواعد Firestore الموحدة لتطبيق WATM

ملف `firestore.rules` هو المصدر الوحيد المعتمد لقواعد قاعدة البيانات.

## النشر من Firebase Console

1. افتح **Firestore Database** ثم **Rules**.
2. احذف نص القواعد القديم من المحرر فقط. لا تحذف Collections أو Documents.
3. انسخ محتوى `firestore.rules` كاملاً والصقه في المحرر.
4. اضغط **Publish** وانتظر عدة ثوانٍ.
5. حدّث تطبيق WATM واختبر إنشاء حساب، المطابقة، تسجيل الحضور، التشجيع، والجدول.

## ما الذي تحميه القواعد؟

- المستخدم يقرأ ويعدّل ملفه وبياناته الفرعية فقط.
- حقول الإدارة والعضوية المدفوعة لا يمكن تغييرها من تطبيق المستخدم.
- الدائرة لا تتجاوز 7 أعضاء، والانضمام يزيد العدد عضواً واحداً فقط.
- أعضاء الدائرة فقط يقرؤون الأعضاء والتشجيعات.
- التشجيع لا يُرسل إلا إلى عضو آخر في الدائرة وبحد أقصى 180 حرفاً.
- المواعيد اليومية والأسبوعية خاصة بصاحب الحساب مع التحقق من الحقول والأوقات.
- أي مسار غير معروف مرفوض تلقائياً.

## ملاحظة إنتاجية

القواعد مصممة للإصدار الحالي الذي ينفّذ المطابقة داخل معاملة Firestore. قبل
الإطلاق التجاري الواسع، يُنصح بنقل إنشاء الدوائر والمطابقة إلى Cloud Functions
حتى تصبح عمليات النظام الحساسة من جهة الخادم فقط.
